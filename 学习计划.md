# FastAPI 学习与面试准备计划

> 目标：通过本项目作为载体，补齐 FastAPI 全栈知识体系，重点针对 **后端开发 + AI 应用方向** 面试。
> 起点：你已经掌握 FastAPI 三层架构、SQLAlchemy 2.0 ORM、Pydantic 基础、CRUD 基本写法。
> 路径文件：[main.py](file:///c:/Users/admin/Desktop/fastAPIserver/main.py)、[routers/student.py](file:///c:/Users/admin/Desktop/fastAPIserver/routers/student.py)、[services/student_service.py](file:///c:/Users/admin/Desktop/fastAPIserver/services/student_service.py)、[schemas/student.py](file:///c:/Users/admin/Desktop/fastAPIserver/schemas/student.py)、[database/models.py](file:///c:/Users/admin/Desktop/fastAPIserver/database/models.py)、[database/database.py](file:///c:/Users/admin/Desktop/fastAPIserver/database/database.py)

---

## 📊 整体路线图

| 阶段 | 周期 | 主题 | 产出 |
|---|---|---|---|
| **P0 面试基础补齐** | 7-10 天 | FastAPI 核心剩余点 + 认证 | 升级版 student 项目，配套 pytest |
| **P1 工程化** | 5-7 天 | 配置/迁移/异步/中间件/日志 | 可上线的小型项目骨架 |
| **P2 AI 后端核心** | 7-10 天 | LLM API + SSE + Prompt | 「AI 聊天接口」项目 |
| **P3 AI 进阶** | 7-10 天 | LangChain + RAG + Agent | 「RAG 知识库问答」项目（简历亮点） |
| **P4 面试冲刺** | 持续 | 八股文 + 刷题 + 简历 | 1 份简历 + 1 套面试题库 |

> 每天建议投入 2-3 小时。每完成一项 ✅，把对应代码 commit 一次。

---

# 🟦 Phase 0 — FastAPI 面试基础补齐（7-10 天）

> 这一阶段目标：补齐你项目里**还完全没碰过的 FastAPI 核心能力**，每个都是面试必问点。

## 0.1 请求参数进阶（1.5 天）

- [✅] 学习 `Path` 参数校验：`Path(..., ge=1, le=1000, description="...")`
- [✅] 学习 `Query` 参数：`Query(default, min_length, max_length, regex, alias)`
- [✅] 学习 `Body` 显式声明、`Embedded`、多个 body
- [✅] 学习 `Header` / `Cookie` / `Form` / `File`
- [✅] 学习 `Optional` / `Union` / `Literal` 类型
- [✅] 学习 `Annotated[类型, Field(...)]` 写法（FastAPI 0.95+ 推荐）

**练习**：
- [✅] 把 [routers/student.py](file:///c:/Users/admin/Desktop/fastAPIserver/routers/student.py) 里所有接口改用 `Path/Query/Body` 显式声明
- [✅] 加一个 `GET /students?limit=10&offset=0&keyword=...` 走分页/搜索
- [✅] 新增 `POST /upload` 接收单个/多个 `UploadFile`
- [✅] 新增 `POST /login-form` 接收 `Form` 表单数据

**产出**：升级版 `routers/student.py`（参数全部显式校验）

## 0.2 响应模型与状态码（0.5 天）

- [✅] `response_model=SchemaClass` 过滤返回字段
- [✅] `response_model_exclude_unset=True` 不返回默认值
- [✅] `status_code=201 / 204` 在路由装饰器上指定
- [✅] `JSONResponse` / `Response` 直接构造

**练习**：
- [ ] 给所有 POST/PUT 加 `response_model` 和 `status_code=201`
- [ ] 写一个统一 `ResponseModel[T]` 包装 `code/msg/data`
- [ ] 删除接口用 `status_code=204`

## 0.3 异常处理（1 天）

- [ ] `HTTPException(status_code, detail, headers)`
- [ ] 自定义异常类（继承 `Exception`）
- [ ] `@app.exception_handler(MyException)` 全局捕获
- [ ] `@app.exception_handler(RequestValidationError)` 改写 422 错误格式
- [ ] 业务码 + HTTP 码分离（`code: 1001` vs `status: 400`）

**练习**：
- [ ] 删 `routers/student.py` 里所有手写 `HTTPException`，改抛业务异常
- [ ] 写 `core/exceptions.py`：`BizException`、`NotFoundException`、`AuthException`
- [ ] 写 `core/exception_handlers.py` 注册到 `main.py`

**面试要点**：能讲清楚「为什么用全局异常处理」和「422 校验错误的内部结构」

## 0.4 依赖注入进阶（1 天）

- [ ] `Depends` 链式（依赖套依赖）
- [ ] `yield` 依赖做资源回收（`get_db` 就是典型）
- [ ] 类作为依赖（`class Pagination: def __init__(self, page, size)`）
- [ ] `use_cache=False`、子依赖复用
- [ ] `Annotated[Session, Depends(get_db)]` 写法

**练习**：
- [ ] 写一个 `get_current_user` 依赖，先返回假数据（不接 DB）
- [ ] 写一个 `Pagination` 类依赖自动解析 `page/size`
- [ ] 把所有 `db: Session = Depends(get_db)` 改成 `Annotated` 写法

## 0.5 认证与授权：OAuth2 + JWT（2 天）⭐⭐⭐

> [dependencies/auth.py](file:///c:/Users/admin/Desktop/fastAPIserver/dependencies/auth.py) 还是空的——这块是面试绝对高频。

- [ ] `OAuth2PasswordBearer(tokenUrl="/token")`
- [ ] `passlib[bcrypt]` 哈希密码
- [ ] `python-jose` 签发/校验 JWT
- [ ] 完整流程：注册 → 登录 → 拿 token → 受保护接口
- [ ] `get_current_user` 依赖从 token 解析 user
- [ ] 角色权限：`require_role("admin")` 装饰器/依赖

**练习**：
- [ ] 新建 `routers/auth.py`：
  - `POST /register`（密码哈希入库）
  - `POST /token`（OAuth2PasswordRequestForm，签发 JWT）
- [ ] 新建 `routers/user.py`：`GET /me` 走 `Depends(get_current_user)`
- [ ] 把学生 CRUD 的 `POST/PUT/DELETE` 加上 `Depends(get_current_user)`
- [ ] 加一个 `require_admin` 依赖，限制 DELETE 接口

**核心代码骨架（参考）**：

```python
# dependencies/auth.py
from datetime import datetime, timedelta
from jose import jwt, JWTError
from passlib.context import CryptContext
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer

SECRET_KEY = "change-me-in-env"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/token")

def hash_password(p: str) -> str:
    return pwd_context.hash(p)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def create_access_token(data: dict, expires_delta: timedelta | None = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=15))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def get_current_user(token: str = Depends(oauth2_scheme)):
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        username: str = payload.get("sub")
        if not username:
            raise JWTError()
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")
    return {"username": username}
```

**面试要点**：
- JWT 三段结构、为什么用 HS256、refresh token 怎么设计
- OAuth2 password flow vs client credentials flow
- 为什么密码要哈希（不存明文）
- token 放 cookie 还是 header

## 0.6 中间件 Middleware（0.5 天）

- [ ] `@app.middleware("http")` 装饰器写法
- [ ] `add_middleware(BaseHTTPMiddleware)` 类写法
- [ ] 用途：请求计时、日志、CORS、限流

**练习**：
- [ ] 写一个请求耗时中间件（`process_time`）
- [ ] 写一个访问日志中间件（打印 method/path/status/time）

## 0.7 CORS 跨域（0.5 天）

- [ ] `from fastapi.middleware.cors import CORSMiddleware`
- [ ] 配置 `allow_origins / allow_methods / allow_headers / allow_credentials`

**练习**：
- [ ] 在 `main.py` 加 CORS，允许 `http://localhost:3000`
- [ ] 写一个 HTML 文件用 fetch 调接口验证

## 0.8 测试（1 天）

- [ ] `from fastapi.testclient import TestClient`
- [ ] pytest 基础：`test_xxx.py`、fixture
- [ ] 覆盖：200、404、422、401

**练习**：
- [ ] 在 `tests/test_student.py` 写 CRUD 全流程
- [ ] 写一个 `conftest.py` 准备测试数据库（`sqlite:///./test.db`）

**产出**：测试覆盖率 ≥ 70%

---

# 🟩 Phase 1 — 工程化（5-7 天）

> 目标：让你的项目从「能跑」变成「能上线」。

## 1.1 配置管理 Pydantic Settings（0.5 天）

- [ ] `pip install pydantic-settings`
- [ ] `class Settings(BaseSettings): DATABASE_URL: str = "sqlite:///./dev.db"; SECRET_KEY: str; DEBUG: bool = False`
- [ ] `.env` 文件管理密钥
- [ ] `@lru_cache` 缓存 `get_settings()`

**练习**：
- [ ] 把 [database/database.py](file:///c:/Users/admin/Desktop/fastAPIserver/database/database.py) 里的硬编码 `DATABASE_URL` 改成从 settings 读
- [ ] 写 `.env` / `.env.example`
- [ ] `.gitignore` 忽略 `.env`

## 1.2 日志（0.5 天）

- [ ] `loguru` 或标准 `logging.config.dictConfig`
- [ ] 写文件 + 控制台双输出
- [ ] 请求 ID 链路追踪

**练习**：
- [ ] 在中间件里加 request_id，并写到日志里
- [ ] 把 `echo=True` 关掉，统一用 loguru 打 SQL

## 1.3 Alembic 数据库迁移（1.5 天）⭐

- [ ] `pip install alembic`，`alembic init alembic`
- [ ] `alembic revision --autogenerate -m "msg"`
- [ ] `alembic upgrade head` / `downgrade -1`
- [ ] 配置 `env.py` 读 `DATABASE_URL`、导入 `Base.metadata`

**练习**：
- [ ] 删掉 `Base.metadata.create_all(bind=engine)`（[main.py 第 7 行](file:///c:/Users/admin/Desktop/fastAPIserver/main.py#L7)），改用 alembic
- [ ] 给 Student 加一个 `email` 字段，生成迁移并执行

**面试要点**：为什么生产不能用 `create_all`？autogenerate 的原理？migration 文件要 commit 吗？

## 1.4 异步 async/await（1.5 天）⭐

- [ ] 路由函数 `async def`
- [ ] 异步 SQLAlchemy：`create_async_engine`、`AsyncSession`
- [ ] `from sqlalchemy.ext.asyncio import async_sessionmaker`
- [ ] 依赖改写：`async def get_db() -> AsyncGenerator[AsyncSession, None]`

**练习**：
- [ ] 把 `get_db` 改成 async 版本
- [ ] 把所有 service 改成 async（`db.execute(select(...))`、`await db.commit()`）
- [ ] 接口里加 `asyncio.sleep(1)` 验证并发

**面试要点**：什么时候用 async？和同步比快多少？Tornado/asyncio 关系？

## 1.5 分页 / 过滤 / 排序（1 天）

- [ ] 手写 `limit/offset`
- [ ] `fastapi-pagination` 库
- [ ] 动态过滤：`Query(None, alias="filter[name]")`

**练习**：
- [ ] `GET /students?page=1&size=10`
- [ ] 返回 `{items, total, page, size, pages}`

## 1.6 Dockerfile + docker-compose（0.5 天）

- [ ] `python:3.11-slim` 基础镜像
- [ ] 多阶段构建（builder + runtime）
- [ ] docker-compose 跑 FastAPI + Redis + Postgres

**练习**：
- [ ] 写 `Dockerfile`、`docker-compose.yml`
- [ ] 用 `uvicorn` 启动并暴露 8000

---

# 🟨 Phase 2 — AI 后端核心（7-10 天）⭐⭐⭐

> 目标：把 FastAPI 用到 AI 场景，做出可演示的「AI 聊天 API」。

## 2.1 环境准备与依赖（0.5 天）

- [ ] `pip install openai httpx tiktoken`
- [ ] 准备 OpenAI / DeepSeek / 通义千问 的 API Key（任选）
- [ ] 写 `.env`：`OPENAI_API_KEY=...`、`OPENAI_BASE_URL=...`

## 2.2 第一个 LLM 接口：非流式对话（1 天）

- [ ] `openai.OpenAI(api_key=..., base_url=...)` 客户端
- [ ] 封装 `llm/chat.py`：`async def chat(messages: list[dict]) -> str`
- [ ] FastAPI 接口 `POST /ai/chat` 接 `{messages: [{role, content}]}`
- [ ] Pydantic Schema 校验入参

**产出文件**：
- `routers/ai.py`
- `services/llm_service.py`
- `schemas/chat.py`

**核心代码骨架**：

```python
# services/llm_service.py
from openai import AsyncOpenAI
from core.config import settings

client = AsyncOpenAI(
    api_key=settings.OPENAI_API_KEY,
    base_url=settings.OPENAI_BASE_URL,
)

async def chat(messages: list[dict], model: str = "gpt-4o-mini") -> str:
    resp = await client.chat.completions.create(
        model=model,
        messages=messages,
    )
    return resp.choices[0].message.content
```

```python
# routers/ai.py
from fastapi import APIRouter
from schemas.chat import ChatRequest, ChatResponse
from services.llm_service import chat

router = APIRouter(prefix="/ai", tags=["ai"])

@router.post("/chat", response_model=ChatResponse)
async def chat_endpoint(req: ChatRequest):
    answer = await chat(req.messages)
    return ChatResponse(answer=answer)
```

## 2.3 流式响应 SSE（2 天）⭐⭐⭐

> AI 对话必须用流式。面试必问。

- [ ] `StreamingResponse` 返回 `async generator`
- [ ] LLM 的 `stream=True` 参数
- [ ] SSE 协议：`data: <chunk>\n\n`、`data: [DONE]\n\n`
- [ ] 前端用 `EventSource` / `fetch + ReadableStream` 接收

**核心代码骨架**：

```python
# routers/ai.py
from fastapi.responses import StreamingResponse

@router.post("/chat/stream")
async def chat_stream(req: ChatRequest):
    async def event_generator():
        stream = await client.chat.completions.create(
            model="gpt-4o-mini",
            messages=req.messages,
            stream=True,
        )
        async for chunk in stream:
            delta = chunk.choices[0].delta.content
            if delta:
                yield f"data: {delta}\n\n"
        yield "data: [DONE]\n\n"
    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

**练习**：
- [ ] 写一个最小 HTML 前端，textarea 输入 + 流式展示
- [ ] 加一个 `cancel` 机制（`request.is_disconnected()` 主动断开 LLM 流）
- [ ] 加 token 计数（`tiktoken`）

**面试要点**：SSE vs WebSocket 区别？流式的好处？长连接超时怎么解决？

## 2.4 Prompt 模板与多轮对话（1 天）

- [ ] `jinja2` 渲染 prompt
- [ ] 系统提示词 system prompt 设计
- [ ] 多轮上下文管理：截断 / token 预算
- [ ] 存储对话历史（先内存，Phase 3 换 DB）

**练习**：
- [ ] 写一个 `prompts/coder.yaml` 存多套 prompt
- [ ] 加一个 `POST /ai/chat?role=coder` 参数切换

## 2.5 限流与成本控制（1 天）

- [ ] `slowapi` 做 IP 限流
- [ ] 按 user 维度限流（依赖 Depends 拿 user）
- [ ] 记录每次调用的 token 消耗，存表

**练习**：
- [ ] 写 `usage_logs` 表：user_id / model / prompt_tokens / completion_tokens / cost
- [ ] 加一个 `GET /admin/usage` 查统计

## 2.6 Function Calling / Tool Use（1.5 天）⭐

> AI 应用后端高频面试题。

- [ ] OpenAI tool calling 协议：`tools=[{"type": "function", "function": {...}}]`
- [ ] 在 FastAPI 里注册工具函数
- [ ] 多步调用：模型 → 工具 → 再喂给模型
- [ ] 用 `@tool` 装饰器（LangChain）或手写

**核心代码骨架**：

```python
# tools/weather.py
def get_weather(city: str) -> str:
    # 真实场景调 API
    return f"{city} 今天晴，25°C"

TOOLS = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "查询某个城市的天气",
            "parameters": {
                "type": "object",
                "properties": {"city": {"type": "string"}},
                "required": ["city"],
            },
        },
    }
]
```

```python
# routers/ai.py
async def chat_with_tools(req: ChatRequest):
    resp = await client.chat.completions.create(
        model="gpt-4o-mini",
        messages=req.messages,
        tools=TOOLS,
        tool_choice="auto",
    )
    msg = resp.choices[0].message
    if msg.tool_calls:
        # 执行工具
        tool_result = get_weather(**json.loads(msg.tool_calls[0].function.arguments))
        # 把结果再喂给模型
        req.messages.append(msg)
        req.messages.append({"role": "tool", "tool_call_id": msg.tool_calls[0].id, "content": tool_result})
        resp2 = await client.chat.completions.create(model="gpt-4o-mini", messages=req.messages)
        return resp2.choices[0].message.content
    return msg.content
```

**产出**：能聊天的「AI 助理」接口，工具能查天气、查时间、查数据库

---

# 🟥 Phase 3 — AI 进阶：LangChain + RAG（7-10 天）⭐⭐⭐

> 这是简历上**最值钱**的项目，建议做成可演示的 demo。

## 3.1 LangChain 基础（2 天）

- [ ] `pip install langchain langchain-openai langchain-community`
- [ ] `ChatOpenAI` / `ChatPromptTemplate` / `StrOutputParser`
- [ ] LCEL 链式语法：`prompt | model | parser`
- [ ] `ChatMessageHistory` 多轮对话
- [ ] LangSmith 追踪（可选）

**核心代码骨架**：

```python
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser

llm = ChatOpenAI(model="gpt-4o-mini")
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个助手"),
    ("user", "{input}"),
])
chain = prompt | llm | StrOutputParser()

# 同步
print(chain.invoke({"input": "你好"}))
# 异步
await chain.ainvoke({"input": "你好"})
# 流式
async for chunk in chain.astream({"input": "你好"}):
    print(chunk, end="", flush=True)
```

**练习**：
- [ ] 把 Phase 2 的 `/ai/chat` 改造成 LangChain 版本
- [ ] 体验 LCEL 链式组合：`{context: retriever, question} | prompt | llm | parser`

## 3.2 RAG 基础 + Chroma（3 天）⭐⭐⭐

- [ ] `pip install langchain chromadb sentence-transformers`
- [ ] Document Loader：`TextLoader / PyPDFLoader / WebBaseLoader`
- [ ] Text Splitter：`RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)`
- [ ] Embeddings：`OpenAIEmbeddings` 或本地 `HuggingFaceEmbeddings(bge-small-zh)`
- [ ] VectorStore：`Chroma.from_documents(docs, embeddings, persist_directory="./chroma_db")`
- [ ] Retriever：`vectorstore.as_retriever(search_kwargs={"k": 4})`
- [ ] RAG Chain：`create_retrieval_chain` / 手写 LCEL

**核心代码骨架**：

```python
# services/rag_service.py
from langchain_community.document_loaders import TextLoader, PyPDFLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_community.vectorstores import Chroma
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_core.output_parsers import StrOutputParser

PERSIST_DIR = "./chroma_db"

def build_kb(file_paths: list[str]):
    docs = []
    for p in file_paths:
        if p.endswith(".pdf"):
            docs += PyPDFLoader(p).load()
        else:
            docs += TextLoader(p, encoding="utf-8").load()
    splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)
    splits = splitter.split_documents(docs)
    embeddings = OpenAIEmbeddings()
    vectordb = Chroma.from_documents(splits, embeddings, persist_directory=PERSIST_DIR)
    return vectordb

def get_qa_chain():
    vectordb = Chroma(persist_directory=PERSIST_DIR, embedding_function=OpenAIEmbeddings())
    retriever = vectordb.as_retriever(search_kwargs={"k": 4})
    prompt = ChatPromptTemplate.from_template("""
基于以下上下文回答问题，如果上下文没有就说"我不知道"。
上下文：{context}
问题：{question}
    """)
    llm = ChatOpenAI(model="gpt-4o-mini", streaming=True)
    chain = (
        {"context": retriever, "question": RunnablePassthrough()}
        | prompt | llm | StrOutputParser()
    )
    return chain
```

**练习**：
- [ ] `POST /ai/kb/upload`：上传 PDF/TXT，构建向量库
- [ ] `POST /ai/kb/ask` 流式问答
- [ ] 跑通：上传一份产品手册 PDF，问相关问题能答出原文
- [ ] 加引用来源：返回 `[{content, source, page}]`

**面试要点**：chunk_size 怎么选？embedding 模型怎么选？召回率怎么评估？什么时候用 hybrid search？

## 3.3 进阶 RAG（2 天）

- [ ] **多文档 / 多租户**：按 user_id 隔离向量库
- [ ] **重排序 Re-rank**：用 `bge-reranker` 对召回结果精排
- [ ] **Query 改写**：用 LLM 把口语化问题改写为检索式问题
- [ ] **对话式 RAG**：把多轮历史压缩为 standalone question
- [ ] **混合检索**：BM25 + 向量（`langchain.retrievers.EnsembleRetriever`）

## 3.4 Agent 入门（1.5 天）⭐

- [ ] LangChain `AgentExecutor` + `create_tool_calling_agent`
- [ ] 自定义工具：`@tool` 装饰器
- [ ] ReAct 框架理解
- [ ] 用 LangGraph 写一个简单 ReAct agent（推荐）

**核心代码骨架**：

```python
from langchain.agents import create_tool_calling_agent, AgentExecutor
from langchain_core.tools import tool
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate

@tool
def add(a: int, b: int) -> int:
    """计算两数之和"""
    return a + b

llm = ChatOpenAI(model="gpt-4o-mini")
tools = [add]
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个数学助手"),
    ("placeholder", "{chat_history}"),
    ("human", "{input}"),
    ("placeholder", "{agent_scratchpad}"),
])
agent = create_tool_calling_agent(llm, tools, prompt)
executor = AgentExecutor(agent=agent, tools=tools, verbose=True)

print(executor.invoke({"input": "3 + 5 等于多少？"}))
```

**练习**：
- [ ] 给 agent 加 3 个工具：计算、查天气、查 RAG 知识库
- [ ] `POST /ai/agent` 入口，能根据问题自动选工具

## 3.5 评估与可观测性（1 天）⭐

- [ ] LangSmith 接入（看 trace）
- [ ] 写 5-10 个测试问题，做问答对
- [ ] 用 `ragas` 做 RAG 评估（context_precision / faithfulness / answer_relevancy）

---

# 🟪 Phase 4 — 面试冲刺（持续）

## 4.1 八股文题库（按主题）

### FastAPI 高频
- [ ] FastAPI 和 Flask 区别？为啥快？（async、Pydantic、Starlette）
- [ ] `async def` 和 `def` 区别？什么场景用哪个？
- [ ] 依赖注入的实现原理？（树形结构解析）
- [ ] Pydantic v2 和 v1 差异？`model_dump` vs `dict`？
- [ ] 中间件执行顺序？
- [ ] `response_model` 怎么过滤字段？安全性？

### 数据库 / SQLAlchemy
- [ ] ORM 三大特性？N+1 问题？怎么解决（`joinedload` / `selectinload`）？
- [ ] session 生命周期？为什么 `Depends(get_db)` 用 `yield`？
- [ ] 事务隔离级别、脏读、幻读？
- [ ] 一级缓存 / 二级缓存？
- [ ] alembic autogenerate 原理？漏掉的情况？

### 认证 / 安全
- [ ] JWT 结构、签名、过期怎么校验？
- [ ] 密码为什么哈希？bcrypt vs argon2？
- [ ] CSRF / XSS / SQL 注入？
- [ ] OAuth2 四种模式？用哪种？
- [ ] refresh token 怎么设计？

### AI / LLM
- [ ] RAG 全流程？embedding 原理？chunk 策略？
- [ ] Function Calling 协议？
- [ ] Prompt Engineering 几大原则？
- [ ] Agent 和 Chain 区别？ReAct 框架？
- [ ] LLM 怎么解决幻觉？
- [ ] SSE / WebSocket 流式区别？
- [ ] token 成本怎么控制？缓存怎么设计（prompt cache / semantic cache）？

## 4.2 简历项目（写 2 段就够）

**项目 1：基于 FastAPI 的学生管理系统（CRUD）**
- 用了 FastAPI + SQLAlchemy 2.0 + Pydantic v2 + JWT 认证
- 涉及三层架构、Alembic 迁移、pytest 测试
- 中间件：请求耗时、CORS、日志

**项目 2：基于 FastAPI + LangChain 的 RAG 知识库问答系统**（重点）⭐
- 用了 FastAPI + OpenAI + LangChain + Chroma
- 流式 SSE 输出、Function Calling、工具调度
- 涉及文档加载、文本切片、embedding、向量检索、rerank
- 接入了 LangSmith 可观测

## 4.3 面试常见环节准备

- [ ] **自我介绍** 1-2 分钟版本，背熟
- [ ] **项目深挖**：每个项目能讲 15 分钟（背景 / 技术选型 / 难点 / 量化结果）
- [ ] **手写代码**：反转链表、二分、SQL 50 题，挑 20 题重点练
- [ ] **系统设计**：短链系统、feed 流、秒杀（选 1-2 个看一遍）

---

# 📅 每日节奏建议

| 时间 | 内容 |
|---|---|
| 0-30 min | 看文档/教程（FastAPI 官方 / LangChain 官方） |
| 30-150 min | 写代码、做项目 |
| 150-180 min | 整理笔记 / 复习八股 / 提交 commit |

---

# ✅ 阶段产出 Checklist

- [ ] **P0 完成**：升级版 student 项目，含 JWT 认证 + 测试
- [ ] **P1 完成**：alembic 迁移、async DB、Dockerfile
- [ ] **P2 完成**：AI 聊天 API（非流式 + 流式 + Function Calling）
- [ ] **P3 完成**：RAG 知识库问答系统（LangChain + Chroma），可演示
- [ ] **P3.5 完成**：简单 Agent 项目
- [ ] **P4 完成**：1 份简历 + 1 套八股文笔记

---

# 📚 推荐学习资源

- FastAPI 官方文档：https://fastapi.tiangolo.com/zh/
- SQLAlchemy 2.0 教程：https://docs.sqlalchemy.org/en/20/
- LangChain 中文文档：https://python.langchain.com/
- 《FastAPI 实战》/《Python Web 开发：测试驱动方法》
- B 站搜「FastAPI 教程」「LangChain 教程」挑播放量高的
- GitHub 搜 `fastapi-template` 借鉴项目结构

---

# 🎯 关键提醒

1. **别只看不写**：每个 checkbox 必须有对应 commit，否则等于没学
2. **AI 部分优先 P2 全部 + P3.2 RAG**：这是面试差异化最大的一块
3. **简历项目要能跑、能演示、能讲 15 分钟**：不要堆技术名词
4. **八股要背，但项目细节更要熟**：面试官追问 90% 是从你项目出发的
5. **P0 阶段所有内容面试必问，缺一不可**

> 写于 2026-07-08，根据你的项目现状定制。可随时根据面试进度调整优先级。
